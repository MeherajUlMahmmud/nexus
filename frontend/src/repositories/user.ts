import { ApiHandler } from './apiHandler';
import { API_ROUTES } from '@/lib/constants';
import {
    LoginRequest,
    RegisterRequest,
    TokenResponse,
    User,
} from '@/lib/types';

export const UserRepository = {
};
